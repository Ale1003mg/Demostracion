namespace Demo.ApiOcelot.Intasller
{
    public class DaoInstalle:IInstaller
    {
        public void InstallServices(IServiceCollection services, IConfiguration configuration)
        {
           // services.AddDAOLayer(configuration);
        }
    }
}
